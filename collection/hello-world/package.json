{
  "name": "npm",
  "version": "1.0.0",
  "description": "hello world",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "(kgrid package hello-world) && (cd ./v0.2.0; kgrid package) && (cd ./v0.3.0; kgrid package)",
    "build": "(cd ./v0.2.0; npm run build) && (cd ./v0.3.0;  npm run  build)",
    "postinstall": "(cd ./v0.2.0; npm install) && (cd ./v0.3.0; npm install)"
  },
  "keywords": [
    "hello",
    "world"
  ],
  "author": "kgrid team",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^7.0.1",
    "klaw-sync": "^6.0.0",
    "scripty": "^1.9.0",
    "shelljs": "^0.8.3"
  }
}
